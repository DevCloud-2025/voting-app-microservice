apiVersion: v1
kind: Pod
metadata:
  name: redis-pod
  labels:
    name: redis-pod
    app: demo-voting-app
spec:
  containers:
    - name: redis
      #image: redis:1.0
      image: lukmanadeokun31/redis:latest 
      ports:
        - containerPort: 6379